<Window x:Class="HighwayCoaster.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HighwayCoaster"
        xmlns:Controls="clr-namespace:HighwayCoaster.Controls"
        mc:Ignorable="d"
        Title="HighwayCoaster"
        DataContext="{Binding Source={StaticResource Locator}, Path=Main}">
    <Window.Style>
        <Style TargetType="Window">
            <Setter Property="ResizeMode" Value="{Binding ResizeMode}" />
            <Setter Property="Height" Value="{Binding WindowHeight, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <Setter Property="MinHeight" Value="450" />
            <Setter Property="Width" Value="{Binding WindowWidth, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <Setter Property="MinWidth" Value="800" />
        </Style>
    </Window.Style>
    <Grid>
        <Grid.Background>
            <VisualBrush>
                <VisualBrush.Visual>
                    <StackPanel Background="White">
                        <MediaElement Stretch="Uniform" Volume="0">
                            <MediaElement.Triggers>
                                <EventTrigger RoutedEvent="MediaElement.Loaded">
                                    <EventTrigger.Actions>
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <MediaTimeline Source="{Binding Background}" RepeatBehavior="Forever"/>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </EventTrigger.Actions>
                                </EventTrigger>
                            </MediaElement.Triggers>
                        </MediaElement>
                    </StackPanel>
                </VisualBrush.Visual>
            </VisualBrush>
        </Grid.Background>
        <ContentControl Content="{Binding WindowContent}"/>
    </Grid>
</Window>
