<UserControl x:Class="HighwayCoaster.Controls.CarSelectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HighwayCoaster.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <SolidColorBrush x:Key="TransparentBlack" Color="#000000" Opacity="0.5" />
    </UserControl.Resources>
    <Grid DataContext="{Binding Source={StaticResource Locator}, Path=CarSelect}">
        <Label Margin ="5 0 5 0"  Content="Select your car" FontFamily="Comic Sans MS Bold" Foreground="Gold" HorizontalAlignment="Center"/>
        <Grid Background="{StaticResource TransparentBlack}"  Margin="25 30 25 100">
            <!--<ItemsControl ItemsSource="{Binding GameLogic.AllCars}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <WrapPanel >
                            <Image Source="{Binding TumbNail}" Height="100"/>
                        </WrapPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel >
                            <Image Source="{Binding TumbNail}" Height="100"/>
                        </WrapPanel>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>-->
            <ListBox ItemsSource="{Binding GameLogic.AllCars}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <Image Source="{Binding TumbNail}"/>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            
            <Border Grid.RowSpan="4" Grid.ColumnSpan="4" BorderBrush="LightGray" BorderThickness="5 5 5 5"/>
        </Grid>
        <StackPanel VerticalAlignment="Bottom" Margin="200,0,200,20" Height="58" MaxWidth="400">
            <Grid Height="50">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="1*"/>
                </Grid.ColumnDefinitions>
                <Button Content="Select" Grid.Column="0" Margin="5 10 5 5" Height="35" Background="DarkOrchid" Foreground="Gold" FontFamily="Comic Sans MS Bold" Command="{Binding SelectCommand}"/>
                <Button Content="Cancel" Grid.Column="1" Margin="5 10 5 5" Height="35" Background="DarkOrchid" Foreground="Gold" FontFamily="Comic Sans MS Bold" Command="{Binding CancelCommand}"/>
            </Grid>
        </StackPanel>
    </Grid>
</UserControl>
