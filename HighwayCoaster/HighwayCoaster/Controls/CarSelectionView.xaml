<UserControl x:Class="HighwayCoaster.Controls.CarSelectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HighwayCoaster.Controls"
             xmlns:converter="clr-namespace:HighwayCoaster.Controls.ControlConverters"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <converter:ImgPathConverter x:Key="ImgPathConverter" />
        <SolidColorBrush x:Key="TransparentBlack" Color="#000000" Opacity="0.5" />
    </UserControl.Resources>
    <Grid DataContext="{Binding Source={StaticResource Locator}, Path=CarSelect}">
        <Label Margin ="5 0 5 0"  Content="Select your car" FontFamily="Comic Sans MS Bold" Foreground="Gold" HorizontalAlignment="Center"/>
        <Grid  >
            <Grid.RowDefinitions>
                <RowDefinition Height="17*"/>
                <RowDefinition Height="177*"/>
                <RowDefinition Height="31*"/>
            </Grid.RowDefinitions>
            <ListView  ScrollViewer.HorizontalScrollBarVisibility="Disabled" Grid.Row="1" Background="{StaticResource TransparentBlack}" ItemsSource="{Binding GameLogic.AllCars}" SelectedItem="{Binding SelectedCar}" Margin="10,10,10,0">
                <ListView.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel />
                    </ItemsPanelTemplate>
                </ListView.ItemsPanel>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Margin="15 0 15 0">
                            <Image Source="{Binding TumbNail, Converter={StaticResource ImgPathConverter}}" MaxWidth="200" MinWidth="25" />
                            <Label Content="{Binding PointRequirement}" ContentStringFormat="Req. points: {0}"  Background="DarkOrchid" Foreground="Gold" FontFamily="Comic Sans MS Bold" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            <Border Grid.Row="1" Grid.RowSpan="1" BorderBrush="Gold" BorderThickness="5 5 5 5" Margin="10,10,10, 0"/>
            <Grid Grid.Row="2" >
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <StackPanel Width="150" HorizontalAlignment="Left"  Margin="10,10,10,10">
                    <Button Content="Select" Height="40"  Background="DarkOrchid" Foreground="Gold" FontFamily="Comic Sans MS Bold" Command="{Binding SelectCommand}"/>
                </StackPanel>
                <StackPanel Grid.Column="1" Width="150" HorizontalAlignment="Right"  Margin="10,10,10,10">
                    <Button  Content="Cancel" Height="40"  Background="DarkOrchid" Foreground="Gold" FontFamily="Comic Sans MS Bold" Command="{Binding CancelCommand}"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
