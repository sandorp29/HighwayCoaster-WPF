<UserControl x:Class="HighwayCoaster.Controls.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             DataContext="{Binding Source={StaticResource Locator}, Path=Login}">
    <UserControl.Resources>
        <SolidColorBrush x:Key="TransparentBlack" Color="#000000" Opacity="0.5" />
    </UserControl.Resources>
    <Grid  Name="MainGrid">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1.4*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        
        <StackPanel Grid.Column="1" VerticalAlignment="Center" MaxWidth="350" MinWidth="250">
            <Grid Margin ="5 5 5 0" Height="100">
                <Grid.Background>
                    <ImageBrush ImageSource="{Binding Logo}"/>
                </Grid.Background>
            </Grid>
            <StackPanel Background="{StaticResource TransparentBlack}" Margin="25 10 25 10">
                <Label Margin ="5 0 5 0"  Content="USERNAME:" FontFamily="Comic Sans MS Bold" Foreground="Gold"/>
                <TextBox Margin ="5 0 5 0"  FontSize="18" Opacity="0.8" Text="{Binding UserName, Mode=OneWayToSource,UpdateSourceTrigger=PropertyChanged}" />
                <Label  Margin ="5 0 5 0"  Content="PASSWORD:" FontFamily="Comic Sans MS Bold" Foreground="Gold"/>
                <PasswordBox x:Name="PasswordBox" Margin ="5 0 5 0" Opacity="0.8" FontSize="18">
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="PasswordChanged">
                            <i:InvokeCommandAction Command="{Binding PasswordChanged}" CommandParameter="{Binding ElementName=PasswordBox}"/>
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                </PasswordBox>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="1*"/>
                    </Grid.ColumnDefinitions>
                    <Button Content="Login" Grid.Column="0" Margin="5 10 5 5" Height="35" Opacity="0.75" Background="DarkOrchid" Foreground="Gold" FontFamily="Comic Sans MS Bold" Command="{Binding LoginCommand}" CommandParameter="{Binding ElementName=PasswordBox}"/>
                    <Button Content="Register" Grid.Column="1" Margin="5 10 5 5" Height="35" Opacity="0.75"  Background="DarkOrchid" Foreground="Gold" FontFamily="Comic Sans MS Bold" Command="{Binding RegisterCommand}"/>
                </Grid>
            </StackPanel>
        </StackPanel>
        <Grid Grid.Column="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*" />
                <ColumnDefinition Width="3*" MaxWidth="250" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="1*" MaxHeight="100"/>
                <RowDefinition Height="4*" />
            </Grid.RowDefinitions>
            <Button Content="Highscores" Margin="10 10 10 10" Grid.Column="2" Opacity="0.75" Background="DarkOrchid" Foreground="Gold" FontFamily="Comic Sans MS Bold" Command="{Binding HighscoreCommand}"/>
        </Grid>
    </Grid>
</UserControl>
